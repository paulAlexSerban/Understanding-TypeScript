/**
 * TASK
 * Use `console.log()` to print "Hello, World!"" on a new line in the console, which is also known as stdout or standard output.
 * Use `console.log()` to print the contents 
 * @param { String } parameterVariable 
 */

function greeting(parameterVariable: string ) {
  console.log(`Hello, World!`);
  console.log(`${parameterVariable} is here, have no fear!!`);
}

greeting('TypeScript');